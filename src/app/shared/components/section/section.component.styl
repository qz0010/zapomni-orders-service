@import "vars.styl"

.b
  padding: 20px 12px;
  background: $c-theme-2;
  box-shadow: $c-shadow-1;
  border-radius: 4px

::ng-deep .device-size
  &-large, &-medium
    app-section
      & > .b
        padding: 40px;
